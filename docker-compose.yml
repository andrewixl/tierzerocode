services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./tierzerocode/:/tierzerocode/
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - ./.env.dev