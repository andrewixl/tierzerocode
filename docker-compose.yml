version: '3.8'

services:
  tierzerocode:
    image: tierzerocode
    build:
      context: .
      dockerfile: ./Dockerfile
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - 8000:8000
    environment:
      - DATABASE_USER=awbadmin
      - DATABASE_PASSWORD=ctd6ayg_jye3bwp@PNQ
      - DATABASE_NAME=tzcdb
      - DATABASE_HOST=db
      - DATABASE_PORT=5432
    depends_on:
      - db
  db:
    image: postgres:16
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=awbadmin
      - POSTGRES_PASSWORD=ctd6ayg_jye3bwp@PNQ
      - POSTGRES_DB=tzcdb
volumes:
  postgres_data: